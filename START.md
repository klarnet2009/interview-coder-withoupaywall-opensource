# 🚀 Как запустить Interview Assistant

## ✅ Сборка успешно завершена!

Все файлы готовы к запуску на вашем Windows-компьютере.

## Запуск на локальном компьютере

### Вариант 1: Через stealth-run.bat (рекомендуется)
```batch
stealth-run.bat
```

### Вариант 2: Вручную
```batch
# 1. Сборка (уже выполнена)
npm run build

# 2. Запуск
npx electron dist-electron/main.js
```

### Вариант 3: Dev режим (с hot reload)
```batch
npm run dev
```

## 🔥 Горячие клавиши

| Действие | Клавиша |
|----------|---------|
| Показать/скрыть окно | `Ctrl + B` |
| Сделать скриншот | `Ctrl + H` |
| Обработать | `Ctrl + Enter` |
| Пауза/продолжить | `Ctrl + Space` |
| Удалить скриншот | `Ctrl + L` |
| Сбросить | `Ctrl + R` |
| Выйти | `Ctrl + Q` |

## 🎨 Что нового (UX Redesign 2025)

### Wizard Onboarding
- **Quick Start** (3 шага) - быстрая настройка за 60 секунд
- **Advanced Setup** (8 шагов) - полная кастомизация
- Выбор провайдера (Gemini/OpenAI/Anthropic)
- Тест API ключа с индикацией статуса
- Создание профиля с загрузкой CV

### Новые компоненты
- `StatusBar` - статус подключения, latency, язык
- `UnifiedInput` - скриншоты + текстовый ввод
- `AIResponse` - ответы AI с quick actions
- `ControlBar` - управление стилем, пауза
- `DebugView` - pipeline logs, тестовый ввод
- `ProfileManager` - управление профилями
- `SessionHistory` - история интервью

## 📁 Структура сборки

```
dist/              ← Frontend (React + Vite)
dist-electron/     ← Backend (Electron main process)
  ├── main.js      ← Точка входа
  ├── preload.js   ← IPC bridge
  └── ...
```

## ⚠️ Важно

При первом запуске появится **Wizard** для настройки:
1. Выберите режим (Quick/Advanced)
2. Выберите AI провайдера
3. Введите API ключ
4. Настройте профиль (опционально)
5. Готово!

Приложение запустится в **невидимом режиме** по умолчанию.
Нажмите `Ctrl + B` чтобы сделать окно видимым.
